ASSUMPTION-1: M2 strategies (pointing/claiming, subsets, fish, wings) are pre-implemented or will be added separately; focus on M3 as specified.
ASSUMPTION-2: Existing M1 strategies (naked/hidden single) are base; M2/M3 register after in difficulty order.
ASSUMPTION-3: For AIC/ALS implementation, use standard definitions from research/08-chains-aic and 09-als; choose AIC engine as primary with named techniques as special cases where possible.
ASSUMPTION-4: Uniqueness techniques optional via config flag default false for soundness in ground-truth.
ASSUMPTION-5: forcing-boundary.md will define simple non-branching AIC depth<=6 as human-acceptable; nets/multi-branch as enumeration to mark/disable.
