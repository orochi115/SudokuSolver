在已完成 P0（高杠杆层）的基础上，继续实现 **P1「标准进阶、中等杠杆」** 一层的新策略，进一步提升 727 解出率。

## 先读
- `docs/plans/diabolical-727-checklist.md` 的 **§P1** 表（本阶段 strategyId 一览）
- 你在 P0 实现的 `strategies/` 代码、`index.ts`、`overlap.ts`、`chain/boundaries.ts`
- `packages/engine/src/strategy.ts`、`trace.ts`
- 研究卡（`research/sudoku-human-solving/local-library/techniques/`）：
  `11-exotic/tridagon.md`、`07-coloring/multi-coloring.md`、`07-coloring/3d-medusa.md`、
  `09-als/als.md`、`09-als/ahs.md`、`06-wings/wxyz-wing.md`、`06-wings/remote-pairs.md`、
  `06-wings/bent-sets.md`、`05-single-digit-patterns/broken-wing.md`、
  `10-uniqueness/avoidable-rectangle.md`、`10-uniqueness/extended-ur.md`、`10-uniqueness/unique-rectangle-bug.md`、
  `08-chains-aic/aic-with-als.md`、`08-chains-aic/aic-with-ur.md`

## 任务
实现文末清单中的全部 P1 strategyId。要点：
- **Tridagon / Multi-Coloring / 3D Medusa**：各 owner（exotic / coloring）；X-Colors/Color Wing/Supercoloring 并入对应 owner（见 overlap）。
- **ALS-Chain / AHS**：`als-chain` 为 ALS owner，`als-xy-wing` 降为其 len-2 特例；`ahs` 作链节点供链引擎复用。
- **WXYZ-Wing / Remote Pairs / Bent Sets / Broken Wing**：wing/bent/oddagon 族；remote-pairs 复用 xy-chain。
- **AR Type 1–4 / Extended UR / Unique Loop·BUG 变体**：uniqueness detector 扩展。
- **AIC with ALS / with UR**：在 AIC 引擎上加节点类型，复用 aic owner。

## 耦合的存量重构（E 项，随本阶段一并完成）
- **E4**：通用 `als-chain` 落地后，把 `als-xy-wing` 降为其特例（alias 或折叠）。
详见 checklist「已有策略调整 backlog」（归类：耦合 P1）。

## 硬性约定
- **必须实现清单中的全部 id**（精确 id 注册）；缺一退回重试。
- 同 P0：不改地基与既有策略行为；策略为纯函数、不改 grid；填好 highlights（含 `links`）与双语讲解；按 difficulty 注册并同步 `CANONICAL_STRATEGY_ORDER`；从 `overlap.ts`/`boundaries.ts` 的 `futureMembers`/reserved 移入正式 members；每策略配单测。
- **在 §P1 表勾选 ✅** 已完成的 strategyId，更新 E4 状态。

## 完成判据（必须全绿）
1. `npm run typecheck` exit 0；`npm test` 全部通过。
2. **健全性**：`data/ground-truth/` 全 400 题零 violation。
3. **727 增量**：`npm run solve:list -- --profile human-default`，记录较 P0 的提升并写进设计说明。
4. 全语料 solved 不回退、不引入 invalid。

## 产出设计说明（必须）
`docs/notes/p1.md`：新增策略与思路；ALS 收编（E4）的取舍；727 增量较 P0 的变化；最难部分与处理方式。
