在已完成 P0–P2 覆盖与 E 阶段存量调整后，本阶段（P3）实现**最后手段 / 红线**策略——forcing chains/nets 与枚举类技巧。**这些只进 `last-resort` profile，绝不进 `human-default`。**

> **红线隔离（硬约束，务必遵守）：** 每实现一个 P3 strategyId，必须把其 id **加入 `packages/engine/src/strategies/profiles.ts` 的 `LAST_RESORT_IDS`**，使它**不出现在** `HUMAN_DEFAULT_STRATEGIES`。727 进度的 human-default 口径**不计** P3。验收含**专门的防污染门控**：若任一 P3 id 出现在 human-default profile，直接判 FAIL。

## 先读
- `docs/plans/diabolical-727-checklist.md` 的 **§P3** 表与其「隔离声明」
- `docs/plans/diabolical-727.md` §有意排除 / §红线（forcing vs 枚举的边界）
- `packages/engine/src/strategies/profiles.ts`（`LAST_RESORT_IDS` 第 31 行）、`test/strategy-profiles.test.ts`
- `packages/engine/src/chain/boundaries.ts`（`forcing-chain` 的 `multiBranch:true` / `profiles:['last-resort']` 声明）
- 现有 `strategies/forcing-chain.ts`（owner，已隔离）

## 任务
实现文末清单中的全部 P3 strategyId（均 last-resort）：
- **Forcing Chains 子类**（digit / nishio / cell / region / DIC）：复用现有 forcing 引擎，按起点/区类型发具名 id。
- **Forcing Net**（cell/region/contradiction/verity 为 kind）：多分支 owner。
- **Kraken Fish**：fish + chains 组合。
- **枚举类**：Tabling、POM、Templates、GEM。

## 硬性约定
- **必须实现清单中的全部 P3 id**（精确 id 注册）；缺一退回重试。
- **每个 P3 id 必须加入 `LAST_RESORT_IDS`**，并在 `chain/boundaries.ts`（若属链类）声明 `profiles: ['last-resort']`；断言它**不在** `HUMAN_DEFAULT_STRATEGIES`（呼应 `strategy-profiles.test.ts`，必要时补断言）。
- 不改地基与既有 human-default 策略行为；策略为纯函数、不改 grid；填 highlights/双语讲解；按 difficulty（9xxx band）注册并同步 `CANONICAL_STRATEGY_ORDER`、与现有 `forcing-chain` 9000 不撞、互不重复。
- 健全性照旧：multi-branch / 枚举类也**不得产生与真解矛盾的消除/落子**。
- **在 §P3 表勾选 ✅**，并据实更新 profile 列。

## 完成判据（必须全绿）
1. `npm run typecheck` exit 0；`npm test` 全部通过（含 profile 隔离测试）。
2. **健全性**：`data/ground-truth/` 全 400 题零 violation。
3. **防污染**：所有 P3 id ∈ `LAST_RESORT_IDS` 且 ∉ `HUMAN_DEFAULT_STRATEGIES`（验收会断言）。
4. **727 口径**：`npm run solve:list -- --profile last-resort` 记录 last-resort 解出率（P3 增量在此体现）；同时跑 `--profile human-default` 确认其解出数**未因 P3 改动而变化**。

## 产出设计说明（必须）
`docs/notes/p3.md`：各 P3 策略的建模（特别是 forcing 引擎如何被多个子类复用）；如何保证健全性与隔离；`solve:list` 在 human-default 与 last-resort 两口径的数字；你对"哪些题只有 last-resort 能解"的观察。
