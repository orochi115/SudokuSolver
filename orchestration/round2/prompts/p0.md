你正在为一个数独交互式教程的**求解引擎**补全人类解法策略。地基已完整：盘面模型、解析器、暴力校验、
`SolveTrace`、`Strategy` 接口、求解循环、健全性校验，以及 **33 个已实现策略**（singles→fish→
wings→coloring→chains/AIC→ALS→uniqueness→sue-de-coq，及 last-resort 的 forcing-chain）。
本阶段（P0）：实现「高杠杆、复用现有机制」一层的新策略，提升对 727 道 diabolical 残题的解出率。

## 先读
- `README.md`、`docs/plans/diabolical-727.md`（路线图与红线）
- `docs/plans/diabolical-727-checklist.md` 的 **§P0** 表（你要实现并勾选的 strategyId 一览）
- `packages/engine/src/strategy.ts`（Strategy 接口）、`trace.ts`（尤其 `Link`，用于链可视化）
- `packages/engine/src/strategies/index.ts`（`STRATEGIES` 注册表 + `CANONICAL_STRATEGY_ORDER`）
- `packages/engine/src/strategies/overlap.ts`、`packages/engine/src/chain/boundaries.ts`（detector 归属 / 共享规则）
- 现有同族参考：`strategies/x-wing.ts`、`x-chain.ts`、`aic.ts`、`unique-rectangle-*.ts`
- 研究卡（`research/sudoku-human-solving/local-library/techniques/`）：
  `04-fish/finned-sashimi.md`、`08-chains-aic/nice-loops.md`、`08-chains-aic/xy-chain.md`、
  `05-single-digit-patterns/turbot-family.md`、`10-uniqueness/unique-rectangle-bug.md`

## 任务
实现本阶段「必须实现的策略」清单中的全部 strategyId（见文末），核心思路：
- **Finned/Sashimi 鱼**：现有 fish 的受控扩展（cover set + fin 格），仅消除"看到全部 fin"的 cover 格；做到 jellyfish 大小为止。
- **Nice Loop / XY-Chain / Turbot Fish**：复用现有链引擎（`x-chain`/`aic` 的 `buildLinkGraph`），按 `boundaries.ts` 的归属注册；turbot-fish 是单数字强链的 presentation 别名（复用 owner，不另写搜索）。
- **Hidden UR / UR Type 3/5/6**：在现有 UR detector 上扩展逐型。

## 耦合的存量重构（E 项，随本阶段一并完成）
- **E6**：`nice-loop` 接管 `AicResult` 现有却未返回的 `*-loop` kind；确保 `aic` 不私自发 loop。
- **E2**：实现 turbot-fish 时评估「单数字强链族统一」（skyscraper/kite/ER 与 turbot 共享 owner，`overlap.unified: false→true`）。
- **E3**：实现 UR 系列时把 3 个独立 UR detector 收敛为共享 UR engine。
详见 checklist「已有策略调整 backlog」的「归类」列（耦合 P0）。

## 硬性约定
- **必须实现清单中的全部 id**（按精确 id 注册）；缺任何一个都会被验收退回重试。
- **不要修改地基**：引擎核心（`grid.ts`/`trace.ts`/`strategy.ts`/`solver.ts`/`soundness.ts`/`bruteforce.ts`/`parser.ts`）、`data/`、顶层配置、既有 spec 文档；**不得删改既有 33 个策略的行为**（`strategy-profiles.test.ts` 会断言注册表与 `CANONICAL_STRATEGY_ORDER` 一致、difficulty 互不重复）。
- 每个新策略 = `strategies/` 下独立纯函数模块，`apply` 返回首个可行步或 null，**不得修改 grid**；填好 `highlights`（含 `links`）与双语 `explanation`（术语对齐 `research/sudoku-human-solving/glossary.zh-en.md`）。
- 在 `index.ts` 的 `STRATEGIES` 按 difficulty 升序注册，并同步更新 `CANONICAL_STRATEGY_ORDER`；复用 detector 的别名按 `overlap.ts` 从 `futureMembers` 移入 `members`。
- 每个策略配单元测试（手工最小盘面或卡内 worked example，断言精确产出 + 整题 sound 不回退）。
- **在 `docs/plans/diabolical-727-checklist.md` §P0 表把已完成的 strategyId 勾选 ✅**，并据实更新「归类」涉及的 E 项状态。

## 完成判据（必须全绿）
1. `npm run typecheck` exit 0；`npm test` 全部通过（含你新增的单测）。
2. **健全性**：`data/ground-truth/` 全部 400 题，`solve()` 产出 trace 经 `checkTraceSoundness` **零 violation**（加回归测试覆盖）。
3. **727 增量**：跑 `npm run solve:list -- --profile human-default`，记录 solved/727 较起点（0/727）的提升；并把数字写进设计说明。
4. 任何改动若降低全语料 solved 数或引入 invalid，立即修正。

## 产出设计说明（必须）
在 `docs/notes/p0.md` 写一份并提交：实现了哪些策略与核心思路；复用了哪些既有 detector（及 overlap/boundaries 的改动）；E2/E3/E6 的处理；`solve:list --profile human-default` 的 727 增量；难点与处理方式。
