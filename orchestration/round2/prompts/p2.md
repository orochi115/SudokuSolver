在已完成 P0、P1 的基础上，实现 **P2「罕见 / 异域、低频」** 一层——这是进入红线枚举前的最后一批人类技巧。

## 先读
- `docs/plans/diabolical-727-checklist.md` 的 **§P2** 表（本阶段 strategyId 一览）
- 你在 P0/P1 的 `strategies/` 代码、`index.ts`、`overlap.ts`、`chain/boundaries.ts`
- 研究卡（`research/sudoku-human-solving/local-library/techniques/`，主要在 `11-exotic/`）：
  `06-wings/wxyz-wing.md`（含 VWXYZ 与翼梯通项）、`11-exotic/exocet.md`、`11-exotic/sk-loop.md`、
  `11-exotic/msls.md`、`11-exotic/fireworks.md`、`11-exotic/aligned-exclusion.md`、
  `11-exotic/subset-exclusion.md`、`11-exotic/sue-de-coq.md`、`08-chains-aic/aic-with-als.md`、
  `04-fish/`（franken/mutant fish 扩展）、uniqueness/对称（gurth）

## 任务
实现文末清单中的全部 P2 strategyId。要点：
- **VWXYZ-Wing**：wing size-ladder 通项，复用 WXYZ 框架。
- **Exocet / SK-Loop / MSLS / Fireworks**：各 exotic owner；SK-Loop 是 MSLS 首发特例。
- **APE / ATE / Subset Exclusion**：subset-exclusion 为 owner，APE/ATE 是对齐特例。
- **Sue de Coq 扩展**：复用现有 sue-de-coq owner，界定与基型差异。
- **AIC with exotic links / Twinned XY-Chains**：AIC 引擎 + exotic 节点 / 复用 xy-chain。
- **Franken / Mutant fish**：fish 扩展（含 Endo Fins / Cannibalism / Siamese 呈现）。
- **Gurth's Symmetrical Placement**：对称占位（uniqueness/对称 owner）。

## 硬性约定
- **必须实现清单中的全部 id**（精确 id 注册）；缺一退回重试。
- 同前：不改地基与既有策略行为；策略为纯函数、不改 grid；填 highlights/双语讲解；按 difficulty 注册并同步 `CANONICAL_STRATEGY_ORDER`；移入 members；每策略配单测。
- **在 §P2 表勾选 ✅** 已完成的 strategyId。

## 完成判据（必须全绿）
1. `npm run typecheck` exit 0；`npm test` 全部通过。
2. **健全性**：`data/ground-truth/` 全 400 题零 violation。
3. **727 增量**：`npm run solve:list -- --profile human-default`，记录较 P1 的提升并写进设计说明。
4. 全语料 solved 不回退、不引入 invalid。

## 产出设计说明（必须）
`docs/notes/p2.md`：异域策略的建模方式与思路；727 增量较 P1 的变化；哪些题仍 stuck、你判断还缺什么策略族；最难部分与处理方式。
