在已完成 M2(T1~T3 基础策略)的基础上,继续实现**高级策略**,使引擎能解开 diabolical 档。

## 先读
- `docs/milestones/M3.md`(本次任务详细需求与验收清单)
- 你在 M2 实现的 `strategies/` 现有代码与 `strategies/index.ts`
- `packages/engine/src/strategy.ts`、`trace.ts`(尤其 `Link` 类型,用于链可视化)

## 任务(建议顺序)
1. 简单染色 Simple Coloring(单数字强链双色,trap/wrap)
2. **强/弱链接图底座 + AIC 引擎**(候选为节点,强弱链为边,支持 grouped links);
   覆盖 X-Chain、XY-Chain、Nice Loops、AIC(Type 1/2、连续/不连续环);
   named 短链特例尽量统一到链框架。
3. ALS:ALS-XZ、doubly-linked ALS、ALS-XY-Wing、ALS 链、Death Blossom
4. 唯一性(标记 optional,以开关控制是否启用):Unique Rectangle、Avoidable Rectangle、BUG/BUG+1
5. 特殊(按需):Sue de Coq、aligned exclusion
6. forcing chains/nets(最后手段):**先成文 `docs/forcing-boundary.md`**,定义何种链算"人类可接受逻辑"、
   何种算"伪装枚举"应禁用/标记;引擎以配置项暴露该边界。

## 硬性约定
- 同 M2:不改地基文件;策略为纯函数;不得修改 grid;填好 highlights(含 `links` 链路径)与双语讲解;
  按 difficulty 注册;每策略配单测。
- 链类务必保证**健全性**:不得产生与真解矛盾的消除/落子。

## 还需新增
- `docs/forcing-boundary.md`(FR-8)
- `docs/flow.md`(FR-9):由策略注册表 + 几个代表性 worked trace 派生的人读流程文档。

## 完成判据(必须全绿)
1. `npm run typecheck` exit 0;`npm test` 全部通过(含 AIC/ALS 的针对性单测)
2. **健全性**:`data/ground-truth/` 全部 400 题(含 diabolical)零 violation
3. 解出率统计:diabolical 档非暴力解出率较 M2 显著提升(验证 AIC/ALS 生效)
4. `docs/forcing-boundary.md`、`docs/flow.md` 成文

技巧规则可参考 `research/sudoku-human-solving/local-library/techniques/`(含 08-chains-aic、09-als 等)。
完成后总结:新增策略、diabolical 解出率变化、链接图设计要点与难点。
