你正在为一个数独交互式教程开发**求解引擎的人类解法策略**。代码库已有完整地基(盘面模型、
解析器、暴力校验、SolveTrace 类型、Strategy 接口、求解循环、健全性校验,以及一个参考策略)。

## 先读
- `README.md`
- `docs/requirements.md`(需求总纲)
- `docs/milestones/M2.md`(本次任务详细需求与验收清单)
- `packages/engine/src/strategy.ts`(必须遵守的 Strategy 接口)
- `packages/engine/src/strategies/naked-single.ts`(Step 构造与 highlights/explanation 的约定模板)

## 任务
实现 `docs/milestones/M2.md` 中列出的 T1~T3 策略:
- T1:hidden single(naked single 已作为参考实现)
- T2:locked candidates(pointing / claiming)、naked/hidden pairs/triples/quads
- T3:基础鱼(X-Wing/Swordfish/Jellyfish,建议用统一 base/cover 模型)、
  单数字模式(Skyscraper / 2-String Kite / Empty Rectangle)、wings(XY/XYZ/W-Wing)

## 硬性约定
- **不要修改地基文件**:`grid.ts`、`trace.ts`、`strategy.ts`、`solver.ts`、`soundness.ts`、
  `bruteforce.ts`、`parser.ts`、`data/ground-truth/`、顶层配置与 `docs/`。
- 每个策略 = `strategies/` 下一个独立纯函数模块,导出符合 `Strategy` 的对象;
  `apply` 返回首个可行步或 null,**不得修改 grid**;必须填好 `highlights` 与双语 `explanation`
  (术语对齐 `research/sudoku-human-solving/glossary.zh-en.md`)。
- 在 `strategies/index.ts` 的 `STRATEGIES` 数组按 difficulty 升序注册。
- 每个策略配单元测试(手工最小盘面,断言精确产出)。

## 还需新增
- `packages/engine/scripts/solve-rate.ts`:对 `data/ground-truth/` 各档跑 `solve()`,
  报告非暴力解出率(产出 `data/reports/solve-rate.json`)。

## 完成判据(必须全绿)
1. `npm run typecheck` exit 0
2. `npm test` 全部通过
3. **健全性**:对 `data/ground-truth/` 全部 400 题,`solve()` 产出的 trace 经 `checkTraceSoundness`
   **零 violation**(请加一个回归测试覆盖此点)
4. 解出率统计脚本可运行并产出报告;easy 期望接近 100%

技巧规则可参考 `research/sudoku-human-solving/local-library/techniques/` 下的技巧卡。
完成后用一段话总结:实现了哪些策略、各档解出率、遇到的难点。
