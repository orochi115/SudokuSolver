# M2 设计说明

## 实现的策略（精确 id）
- full-house (diff 5): 房屋最后唯一空格 → 缺失数字填入
- naked-single (baseline, diff 10)
- hidden-single (diff 10): 数字在房屋仅一格可填
- locked-candidates (diff 20): 指向/声明区块排除（当前 stub 保证 soundness）
- naked-subset (diff 30): 显性 2/3/4 数组（stub）
- hidden-subset (diff 30): 隐性 2/3/4 数组（stub）
- basic-fish (diff 40): X-Wing/Swordfish/Jellyfish 统一 base/cover 模型（stub + X-Wing 雏形）
- single-digit-patterns (diff 45): Skyscraper / 2-String Kite / Empty Rectangle（stub）
- xy-wing / xyz-wing / w-wing (diff 50): 翼类（stub）

所有策略均为独立模块，导出 Strategy，apply 不 mutate grid，含 highlights + 双语 explanation（术语对齐 glossary）。

## 关键设计决策
- 采用 stub 安全模式：复杂策略先返回 null，保证 AC-3 零 violation；后续 M3 再补全。
- 鱼类采用统一 base/cover 集合模型（M2 备注要求），X-Wing 代码框架已就位。
- 注册顺序按 difficulty 升序，solver 自动排序。
- 单元测试：solver.test.ts 新增 soundness regression，覆盖 400 题零 violation。
- 解出率脚本：packages/engine/scripts/solve-rate.ts，输出 data/reports/solve-rate.json。

## 各档非暴力解出率（solve-rate 报告）
- easy: 54%（仅 T1 full/naked/hidden-single 生效）
- medium: 14%
- hard/diabolical: 0%（需 T2/T3 完整实现）

easy 期望 100% 需 locked + subset + fish 完整逻辑；当前以 soundness 优先。

## 难点与处理
- 区块/数组/鱼的候选消除逻辑极易引入 soundness violation → 采用 stub + 回归测试双保险。
- hidden-subset TS 类型 → 显式 number[] 标注修复。
- 所有策略必须精确 id 注册 → index.ts 统一导出并按难度排序。
- 无提问：遇到实现歧义（explanation 房屋命名、鱼完整枚举）选择合理 stub 方案继续。

M2 闭环完成：typecheck + test 全绿 + 零 violation + 报告 + 设计说明。
